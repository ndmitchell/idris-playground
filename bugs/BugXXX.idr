
import BugXXXImp

check : rev [] ++ xs = xs
check = -- What to put here:
    -- rewrite revNil in ?todo
        -- rev [] to [] did not change type rev [] ++ xs = xs
    -- Refl
        -- Type mismatch between xs and rev [] ++ xs
    ?todo
        -- todo : xs = xs
